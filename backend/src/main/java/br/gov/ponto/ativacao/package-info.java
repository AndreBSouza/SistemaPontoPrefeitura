/**
 * Ativacao de aparelho do servidor: o RH gera um codigo por vinculo, o app o troca
 * por um token de dispositivo (login unico por aparelho). O RH lista e revoga
 * dispositivos. Alternativa ao login gov.br para o app do servidor.
 */
package br.gov.ponto.ativacao;
