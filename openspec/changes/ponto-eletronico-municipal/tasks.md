## 1. Fundações e Infraestrutura (F0)

- [ ] 1.1 Provisionar contas de nuvem (região Brasil) e organização GitLab; definir ambientes dev/staging/prod — *externa: IaC pronta (AWS sa-east-1) em `infra/terraform/` com tfvars dev/staging/prod; falta executar com a conta/credenciais do cliente (custo)*
- [x] 1.2 Escrever IaC base com Terraform (rede/VPC, Kubernetes, PostgreSQL HA, Redis, RabbitMQ, object storage) — *`infra/terraform/` (VPC, ECS/EKS, RDS Multi-AZ, ElastiCache Redis, Amazon MQ, S3 cifrado, ECR) + Secrets Manager + tfvars por ambiente*
- [x] 1.3 Configurar Keycloak (realms, perfis: servidor/gestor/RH/controladoria/tenant-admin/operador, MFA) — *realm `ponto`: 6 perfis, clients (PKCE/bearer-only), **MFA OTP condicional** p/ perfis admin e IdP gov.br (placeholder)*
- [x] 1.4 Criar esqueleto do backend Spring Boot 3 modular (Spring Modulith) com módulos vazios e OpenAPI — *compila, sobe e passa no teste de contexto; 12 módulos como pacotes + tenant context + OpenAPI/Swagger*
- [x] 1.5 Criar esqueleto do app Flutter e do web React+TS (design system base) — *web React+TS buildando (`npm run build` OK); Flutter 3.44.3 + Android SDK instalados, app scaffoldado (`flutter analyze` limpo) e **`app-debug.apk` buildado com sucesso** (Gradle assembleDebug, exit 0). Design system base e plugins (ver `mobile/pubspec.planned.yaml`) entram nas tasks 5.x. iOS exige Mac/CI.*
- [x] 1.6 Configurar CI/CD GitLab (build, testes, SAST, scan de dependências, deploy por ambiente) — *`.gitlab-ci.yml` (build/test + SAST + dependency/secret scanning + deploy staging/prod) + GitHub Actions (backend, Android, **iOS em runner macOS**)*
- [x] 1.7 Configurar observabilidade (Prometheus/Grafana/Loki/Sentry) e gestão de segredos (Vault/SSM) — *stack `infra/observability/` (Prometheus/Grafana/Loki/Promtail) + `/actuator/prometheus` (micrometer); segredos doc em `docs/secrets-management.md`*

## 2. Multi-tenancy e IAM

- [x] 2.1 Modelar tenant e contexto de tenant (resolução via claim do token) — *Tenant + TenantContext implementados; resolução via header `X-Tenant-Id` por ora (claim do token chega com o IAM, 2.3)*
- [x] 2.2 Implementar isolamento por RLS no PostgreSQL + filtro de aplicação por tenant — *RLS (FORCE) nas tabelas + `TenantAwareDataSource` (GUC) + filtro de aplicação por tenant_id; isolamento verificado em teste*
- [x] 2.3 Implementar autenticação OIDC (Spring Security) e autorização por perfil (RBAC) — *resource server JWT (Keycloak) + RBAC por endpoint + roles de realm → authorities + tenant via claim `tenant_id`; testado (401/403/200)*
- [x] 2.4 Exigir MFA para perfis administrativos — *fluxo `browser-mfa` no Keycloak com OTP condicional por role admin (rh/controladoria/tenant-admin/operador)*
- [x] 2.5 Federação gov.br — **REMOVIDA a pedido**. Autenticação final, sem gov.br: servidor pelo app via **código de ativação + token de dispositivo** (`X-Device-Token`); administradores pelo **Keycloak corporativo** (usuário/senha do realm + MFA OTP). Realm com `identityProviders: []`.
- [x] 2.6 Testes automatizados de vazamento entre tenants — *`MultiTenancyCadastroTest` (PostgreSQL embarcado) valida isolamento e unicidade de CPF por tenant*

## 3. Cadastro de servidor e vínculos

- [x] 3.1 Modelar servidor, vínculo/matrícula (regimes), lotação e organograma — *entidades Servidor/Vinculo/Lotacao + migration V1; organograma hierárquico (unidade-pai) fica para evolução*
- [x] 3.2 CRUD de servidores com unicidade de CPF por tenant — *POST/GET `/api/servidores` + regra de CPF único por tenant (testado)*
- [x] 3.3 Múltiplos vínculos por servidor com cargo/carga horária próprios — *vínculos no cadastro do servidor, com matrícula única por tenant*
- [x] 3.4 Hierarquia de aprovação a partir da lotação/chefia — *lotação com chefia (`/api/lotacoes`), vínculo lotado, e `/api/justificativas/chefia/{id}/pendentes` (testado)*
- [x] 3.5 Importação em lote (planilha/integração) com relatório de erros por linha — *`POST /api/servidores/importar` (CSV); cada linha em transação própria, erros por nº de linha (testado). **Agora lota por órgão:** coluna `lotacaoSigla` por linha (órgãos diferentes no mesmo arquivo) ou parâmetro `?lotacaoId` (órgão padrão) → o vínculo já entra lotado e herda as regras do órgão; web lê o CSV como texto e envia o órgão selecionado. `ImportacaoServidorTest`.*

## 4. Jornadas e escalas

- [x] 4.1 Modelar jornadas (fixa, flexível, 12x36, plantão, magistério) com tolerâncias e intervalo — *entidade Jornada + enum TipoJornada + migration V2; horários por dia da semana adicionados (migration V4, `PUT /api/jornadas/{id}/horarios`)*
- [x] 4.2 CRUD de jornadas e validações de consistência — *POST/GET `/api/jornadas` + nome único por tenant + validação de carga/tolerância/intervalo*
- [x] 4.3 Atribuição de escala ao vínculo por vigência, com checagem de sobreposição — *POST/GET `/api/escalas` + rejeição de sobreposição de vigência (testado)*
- [x] 4.4 Escalas rotativas e troca de turno — *troca de turno (`POST /api/escalas/trocar-turno`, swap de jornada entre escalas, testado); rotação por padrão automático fica como evolução*

## 5. Registro de ponto (núcleo — F1)

- [x] 5.1 Endpoint de registro com geração de NSR imutável e comprovante — *`POST /api/registros` + NSR sequencial por tenant (upsert atômico) + ComprovanteResponse; testado*
- [x] 5.2 Idempotência de registro (chave de deduplicação) para sincronização — *idempotencyKey única por tenant; reenvio retorna o mesmo registro (testado)*
- [x] 5.3 App: fila local cifrada (Drift/SQLite) e fluxo offline-first — *offline-first via fila local (`OfflineQueue`); cifragem da fila (secure storage/Drift) pendente*
- [x] 5.4 App: sincronização em background com tratamento de conflitos — *`SyncService` + `connectivity_plus`; deduplicação por idempotencyKey no backend evita duplicidade*
- [x] 5.5 Geolocalização e geofencing por local de trabalho — *`GeoService` (geolocator) captura posição + `dentroDaCerca`; cerca por local depende de cadastro do local de trabalho (pendente)*
- [x] 5.6 Biometria facial com liveness on-device (ML Kit) e **comparação 1:1**: liveness/captura prontos; comparação implementada em `ComparadorFacial` (similaridade de cosseno + limiar, POJO testável) + `BiometriaService.verificar` + `POST /api/me/verificar-face` (servidor derivado do dispositivo); `referencia` ampliada p/ embedding (migration V17). *A EXTRAÇÃO do descritor (embedding) exige modelo de reconhecimento (TFLite/FaceNet on-device ou Rekognition) — parte externa.*
- [x] 5.7 Cadastro da referência biométrica com consentimento (LGPD) — *`POST /api/biometria/referencia` exige consentimento vigente (testado); guarda template/hash (minimização)*
- [x] 5.8 Registro via web e totem (PWA) — *web vira PWA (manifest) + rota `/totem` (quiosque) registrando com origem TOTEM; `npm run build` OK*
- [x] 5.9 Carimbo de tempo do servidor e marcação auditável de "registro offline" — *dataHoraServidor (carimbo do servidor) + dataHoraDispositivo + flag offline persistidos*

## 6. Apuração de frequência

- [x] 6.1 Motor de apuração: comparar marcações × escala vigente — *`ApuracaoService.apurarDia` + `GET /api/apuracao/dia`; usa horários por dia da jornada (migration V4)*
- [x] 6.2 Ocorrências automáticas (atraso, saída antecipada, falta, hora extra, adicional noturno) — *implementadas e testadas (atraso/falta/hora extra com cenários; adicional noturno por janela 22h–05h)*
- [x] 6.3 Banco de horas (crédito/débito, limites, compensação/prescrição) — *ledger `banco_horas_lancamento`; crédito de hora extra / débito de atraso a partir da apuração, limite de acúmulo, compensação (valida saldo) e prescrição; `/api/banco-horas/*` (testado)*
- [x] 6.4 Abonos/justificativas com anexo (atestado) e workflow de aprovação — *`/api/justificativas` (solicitar/aprovar/rejeitar/pendentes); status PENDENTE→APROVADA/REJEITADA; anexo como referência (upload p/ object storage depois)*
- [x] 6.5 Reflexo das aprovações/rejeições na apuração, com histórico — *justificativa aprovada que cobre o dia neutraliza FALTA/ATRASO/SAÍDA_ANTECIPADA; flag `justificado` (testado)*

## 7. Espelho, assinatura e fechamento

- [x] 7.1 Geração do espelho mensal por vínculo/competência — *`GET /api/espelho` consolida apuração diária + totais + status da competência*
- [x] 7.2 Ciência/assinatura eletrônica do servidor com captura de evidências — *`POST /api/espelho/ciencia` registra cienciaEm + evidência; assinatura ICP-Brasil avançada depois*
- [x] 7.3 Fechamento de competência com bloqueio de edição — *`POST /api/espelho/fechar`; bloqueia novas justificativas no período; re-fechamento rejeitado (testado)*
- [x] 7.4 Reabertura controlada e auditada — *`POST /api/espelho/reabrir` com motivo + reabertoEm; trilha completa de auditoria virá em 8.2*

## 8. Relatórios, auditoria e conformidade (F3)

- [x] 8.1 Relatórios e dashboards (absenteísmo, atrasos, horas extras, banco de horas) com exportação — *`/api/relatorios/frequencia` (+ CSV) agrega atrasos/faltas/hora extra/justificados; dashboards são UI sobre estes dados*
- [x] 8.2 Trilha de auditoria imutável (registro, tratamento, aprovação, fechamento, acesso a dado sensível) — *`auditoria_evento` (só inserção) registrada em registro de ponto, decisão de justificativa, fechamento e reabertura; `/api/auditoria`*
- [x] 8.3 Geração de AFD e AEJ no layout da Portaria MTP 671/2021 com assinatura de integridade — *AFD (`/api/relatorios/afd`) **e AEJ** (`/api/relatorios/aej`), ambos com hash SHA-256; layout representativo a validar vs leiaute oficial*
- [x] 8.4 Relatório de controle de frequência para a Controladoria (IN 008/2021) — *`/api/conformidade/in008` (totais de servidores/vínculos/registros como evidência)*
- [x] 8.5 Controles LGPD: consentimento, retenção/descarte, atendimento a direitos do titular — *`/api/lgpd` consentimento + exportação + eliminação (anonimização; CPF/registros retidos por obrigação legal)*
- [x] 8.6 DPIA/RIPD do tratamento de biometria/geolocalização — *documento [`docs/dpia-ripd.md`](../../../docs/dpia-ripd.md)*

## 9. Plataforma SaaS e integrações (F4)

- [x] 9.1 Onboarding/provisionamento de tenant com parâmetros iniciais — *`POST /api/onboarding` cria ente + lotação inicial (gravada já no contexto do novo tenant)*
- [x] 9.2 Planos e apuração de consumo (servidores ativos/mês) para faturamento — *`/api/billing` define plano e apura fatura (ativos × preço)*
- [x] 9.3 Conector/exportação para folha de pagamento (CSV/posicional + conector) — *`GET /api/integracoes/folha/csv` (insumos por vínculo); conector por sistema específico depois*
- [x] 9.4 Integração com eSocial (dados de jornada, vínculos celetistas) — *`GET /api/integracoes/esocial/jornada` (representativo; validar vs leiaute vigente)*
- [x] 9.5 Notificações push (FCM) e e-mail (lembretes, pendências, alertas) — *`/api/notificacoes` + ledger + interface `Notificador` (dispatch FCM/SES por trás; impl atual registra em log)*

## 10. Qualidade, segurança e go-live (F5)

- [ ] 10.1 Cobertura de testes e carga — *parcial: **72 testes verdes** (unit puros + integração Postgres embarcado + **e2e do fluxo completo** `FluxoCompletoE2eTest`, que pegou um bug real de auditoria no fluxo por dispositivo); **teste de carga** ainda pendente (precisa infra + Gatling/JMeter)*
- [ ] 10.2 Pentest e correção de vulnerabilidades; hardening final — *externa: checklist em `docs/pentest-checklist.md`; execução do pentest é atividade humana/contratada*
- [ ] 10.3 Acessibilidade (WCAG 2.1 AA / eMAG) na web e diretrizes no app — *parcial: diretrizes em `docs/acessibilidade.md`; painel web em **DSGov** (HTML semântico, foco visível, contraste AA) e app com **tema acessível para idosos** (fontes grandes, alto contraste, alvos amplos); auditoria WCAG formal ainda pendente*
- [ ] 10.4 Publicação nas lojas (Apple App Store, Google Play) e fichas técnicas — *externa: workflows CI (Android/iOS) prontos; publicação exige contas Apple/Google + fichas*
- [ ] 10.5 Backups/DR validados (PITR, restore, runbook) — *runbook em `docs/runbook-dr.md`; validação real exige a infra provisionada (1.1)*
- [ ] 10.6 Piloto em 1 ente (produção assistida) e ajustes — *externa: plano em `docs/plano-piloto.md`; execução exige ente real + produção*
- [ ] 10.7 Go-live e plano de suporte/operação — *externa: runbook em `docs/runbook-go-live.md`; go-live exige infra + decisão humana*

## 11. Camada de UI/UX — produto (botão único, Órgão, app do servidor, painel admin)

- [x] 11.1 Ponto inteligente (botão único): `POST /api/registros/bater` deduz o tipo (entrada/intervalo/saída) pela sequência do dia + jornada; regra em POJO testável (`DeducaoBatida`), `BatidaResponse` com mensagem amigável ("Entrada registrada às 08:03"), mantendo NSR + idempotência + auditoria — *`DeducaoBatidaTest` (unit) + `RegistroPontoTest#bater*` (integração Zonky) verdes*
- [x] 11.2 Órgão de 1ª classe: `Lotacao` enriquecida com regras próprias (jornada padrão, tolerância, banco de horas, geofence lat/long/raio) via `RegrasPonto` (`@Embeddable`) + migration V14; `PUT /api/lotacoes/{id}/regras` + `LotacaoResponse` enriquecido; herança resolvida em `RegrasPontoService` (override do vínculo via escala → regras do órgão → defaults), consumida pelo `/bater` — *`OrgaoRegrasTest` (integração) verde*
- [x] 11.3 App do servidor (Flutter, acessível p/ idosos): tema alto contraste + fontes grandes (`app_theme.dart`); **botão único** "Registrar ponto" → `/bater` com feedback em letras grandes; identificação funcional persistida (path_provider); telas Meu espelho, Meus comprovantes, Solicitar justificativa (anexo→RH), Banco de horas, Notificações; login gov.br (placeholder); offline-first via fila de "bater" + sync FIFO — *`flutter analyze` limpo*
- [x] 11.4 Painel administrativo (React + DSGov): shell gov.br (header/menu lateral/breadcrumb), login dev por `X-Tenant-Id` (seam OIDC documentado), cliente de API tipado com tratamento de erro; páginas Órgãos (editor de regras de ponto), Servidores/Vínculos (+ import CSV), Jornadas (+ horários)/Escalas, Atestados (aprovar/recusar), Espelho/Fechamento, Banco de horas, Relatórios/Conformidade (IN 008), Folha/eSocial, Auditoria, Billing, Dashboard — *`npm run build` verde*
- [x] 11.5 Geofence no registro: `POST /api/registros/bater` sinaliza `foraDaCerca` quando o órgão tem cerca e a localização está fora do raio — `Geofence` (POJO/Haversine) + coluna `fora_da_cerca` (migration V15), exposto em `BatidaResponse`/`ComprovanteResponse`; **marca para tratamento, não bloqueia** (registro imutável). Sinalização exibida no **app** (faixa âmbar "fora da área") e no **painel** (badge em Auditoria + nota no editor de regras do órgão). Modo bloqueante por política do ente fica como toggle futuro — *`GeofenceTest` + caso em `OrgaoRegrasTest` verdes*
- [x] 11.6 Regras do órgão na apuração: **tolerância do órgão** tem precedência sobre a da jornada (`ApuracaoService`); **banco de horas** respeita `bancoHorasHabilitado` do órgão (`BancoHorasService.lancarApuracaoDoDia` não lança se desabilitado), via `RegrasPontoService.regrasDoOrgaoDoVinculo` — *`RegrasOrgaoApuracaoTest` verde (tolerância + gate)*
- [ ] 11.7 Login real gov.br/Keycloak (OIDC) no app e no painel — hoje há o seam dev (X-Tenant-Id / `--dart-define`); *externa: credenciais gov.br*
- [x] 11.8 Autenticação por **código de ativação do RH** + *device binding* (login único por aparelho; alternativa ao gov.br): backend módulo `ativacao` (`CodigoAtivacao` + `Dispositivo`, migration V16; `POST /api/ativacao/codigos` e `/ativar`, `GET`/`POST .../dispositivos[/{id}/revogar]`; `DeviceAuthFilter` resolve tenant+vínculo pelo header `X-Device-Token`, revogado → 401) — *`AtivacaoTest` verde*. **Painel**: gerar código (exibido grande + copiar) e listar/revogar dispositivos por vínculo (em Servidores). **App**: tela de ativação por código → token em `flutter_secure_storage` → abre direto no botão, sem relogar. *Em produção, integrar o device token à cadeia do Spring Security (hoje vale no perfil dev/open).*
- [x] 11.9 Lembrete de batida: notificações **locais agendadas** (offline) pelos horários da jornada do vínculo, com **som próprio alto e característico** (`alerta_ponto`; canal Android `ponto_lembretes` importância MAX + iOS `alerta_ponto.wav`) — **não** o som padrão do aparelho; ao **tocar** o lembrete abre o liveness facial e bate (`/bater`). Manifest com `POST_NOTIFICATIONS`/`RECEIVE_BOOT_COMPLETED` + receivers do plugin; *core library desugaring* habilitado. *iOS: `alerta_ponto.wav` já adicionado ao target Runner (pbxproj); confirmar no build Mac/CI.*
- [x] 11.10 Endurecimento p/ produção: `DeviceTokenAuthenticationFilter` integra o token de dispositivo à cadeia do **Spring Security** (ROLE_servidor; vale em produção, não só dev); facade **`/api/me/*`** (bater/jornada/comprovantes/espelho/banco-horas/justificativas/notificações) deriva o vínculo do dispositivo → o servidor só acessa os próprios dados; app repontado para `/api/me/*` — *`MeAuthTest` verde; suíte 66/66; `flutter analyze` limpo; `flutter build apk` OK*
- [x] 11.11 Verificação ponta-a-ponta (stack no ar: Postgres + backend dev + web Vite): login dev, Dashboard, **Órgãos**, **Servidores** e **geração de código de ativação** validados ao vivo — screenshots `01-login`…`05-codigo-ativacao` na raiz do projeto. **App Android** verificado no emulador (AVD ponto): APK atual instalado, permissão de notificações, **ativação por código** (`477R-29UG` → dispositivo criado no backend, `/api/ativacao/ativar`) e **botão único "Registrar ponto"** — screenshots `app-03`…`app-11`.
- [x] 11.12 **Sem gov.br** (a pedido): IdP `govbr` removido do realm (`identityProviders: []`) e da doc; tela/menu "Entrar com gov.br" removidos do app; nota do painel atualizada. Auth = código/dispositivo (servidor) + Keycloak corporativo (admin).
- [x] 11.13 **Assinatura de release**: keystore própria (`android/app/ponto-release-key.jks` + `android/key.properties`, fora do git via `.gitignore`) + `signingConfig` release no Gradle. `ponto-municipal-app-release.apk` (~80 MB) assinado com `CN=Ponto Municipal` (verificado por `apksigner`), não mais a chave de debug. *Play Store: trocar por keystore definitiva + applicationId final.*
- [x] 11.14 **Biometria 1:1**: `ComparadorFacial` (cosseno + limiar, POJO testável) + `BiometriaService.verificar` + `POST /api/me/verificar-face` (servidor derivado do dispositivo); `referencia` ampliada p/ embedding (migration V17). *Extração do embedding = modelo externo.*
- [x] 11.15 **e2e + correção**: `FluxoCompletoE2eTest` (HTTP + device auth → 4 batidas deduzidas → comprovantes → espelho) revelou e corrigi um overflow de auditoria no fluxo por dispositivo (`DispositivoAuthentication.getName()` curto). Suíte **72/72** verde.
- [x] 11.16 **Endurecimento e melhorias (top 5 + extras)** — suíte **89/89** verde, web `npm run build` e `flutter analyze` limpos:
  - **Cripto em repouso (LGPD)**: `CriptografiaService` (AES-256-GCM, chave por env `PONTO_CRYPTO_SECRET`) cifra o template biométrico (`ReferenciaBiometrica.referencia`, cifra/decifra em `BiometriaService`); fila offline do app migrada de arquivo-texto para `flutter_secure_storage` (cifrada pelo SO). *`CriptografiaServiceTest`.*
  - **Rate-limit na ativação**: `RateLimiterJanela` (POJO testável) + `LimiteAtivacao` (10/min por IP) no `POST /api/ativacao/ativar` → HTTP 429. *`RateLimiterJanelaTest`.*
  - **Hash-chain do NSR (tamper-evidence, REP-P)**: cada registro carrega `hash`=SHA-256(hash_anterior + campos imutáveis) (migration V18, POJO `CadeiaHash`), encadeado por tenant no `RegistroService.salvar`. *`CadeiaHashTest` + `RegistroPontoTest#registrosFormamCadeiaDeHash`.*
  - **Cache do device token**: `CacheDispositivo` (TTL 30s) evita consulta ao banco a cada request autenticada; revogação invalida na hora. Prod → Redis (já na compose). *`CacheDispositivoTest` + `AtivacaoTest`.*
  - **Geofence como verificação silenciosa do administrador** (revisado em L21): a localização do órgão (`geofenceLatitude/Longitude/RaioMetros`, opcional) **nunca bloqueia nem alerta o servidor**; quando configurada, a batida grava lat/lng e o indicador `foraDaCerca` no registro de ponto, exibido só para o admin conferir (badge "fora da área" + link Google Maps na tela "📍 Localização" do painel). Teletrabalho desativa a sinalização. Migrations V19 (adicionou `geofence_bloqueia`) → **V37 (removeu `geofence_bloqueia`)**; a política de bloqueio e a ocorrência `FORA_DE_AREA` **não existem mais**. App mantém só a recusa de **localização simulada** (`Position.isMocked`). *Casos em `RegrasOrgaoApuracaoTest`: `batidaForaDaCercaNaoPenalizaNaApuracao`, `baterForaDaCercaNuncaBloqueiaApenasRegistraParaAdmin`, `teletrabalhoIgnoraGeofence`.*
  - **Isolamento multi-tenant** (complementa 2.6): `MultiTenantIsolamentoTest` prova que o ente B não enxerga registros/dispositivos de A nem gera código p/ vínculo de A.
  - **Teto de banco de horas por órgão**: `RegrasPonto.tetoBancoHorasMinutos` (migration V20) — `BancoHorasService` usa o teto do ente (default do sistema 200h) ao lançar/compensar; form no painel. *`RegrasOrgaoApuracaoTest#tetoBancoHorasDoOrgaoLimitaAcumulo`.*
  - **AFD (Portaria 671/2021)** reescrito para **largura fixa**: cabeçalho tipo 1 (CNPJ do ente + razão social + datas), marcações tipo 3 (NSR/data/hora/CPF), trailer tipo 9 (contagem por tipo) + hash SHA-256; `tenant.cnpj` (migration V21); `GET /api/relatorios/afd/arquivo` baixa o `.txt`. Assinatura **ICP-Brasil** é etapa externa documentada em `docs/afd-assinatura-icp-brasil.md`. *layout verificado em `ConformidadeTest`.*
- [x] 11.17 **Restante codável + docs de implantação** — backend **91/91**, `vitest` 3/3, `flutter test` 3/3, `flutter analyze` limpo, `npm run build`/build OK:
  - **LGPD autoatendimento no app**: `GET /api/me/lgpd/dados` + `GET/POST /api/me/lgpd/consentimento` (servidor derivado do dispositivo) + tela "Privacidade (LGPD)" (ver dados + toggle de consentimento de biometria). *`MeAuthTest#lgpdAutoatendimentoPeloApp`.*
  - **Espelho em PDF**: `PdfEspelhoService` (OpenPDF) + `GET /api/relatorios/espelho/pdf`. *`ConformidadeTest#espelhoEmPdf`.*
  - **Assinatura ICP-Brasil (ponto de plugue)**: `AssinaturaService` + `AssinaturaIndisponivel` (no-op) + `AfdResponse.assinatura`; prod fornece bean `@Primary` (CAdES/BouncyCastle). `docs/afd-assinatura-icp-brasil.md`.
  - **Testes de frontend**: web Vitest (`apiFetch`/erro/tenant) + Flutter (`DadosTitular.fromJson` + widget de consentimento).
  - **Particionamento de `registro_ponto`**: estratégia + template SQL fora do Flyway (`db/manual/`) — `docs/particionamento-registro-ponto.md` (não auto-aplicado: muda a PK + exige rotina de manutenção; tarefa de DBA).
  - **Passo a passo de implantação** dos itens externos (OIDC, Redis, FCM, ICP, facial, nuvem, lojas, CI, carga, WCAG): `docs/passo-a-passo-implantacao.md`.
- [x] 11.18 **Verificação configurável + canais (app/web) + prompt de design** — backend **93/93**, web `build`/`vitest` OK, `flutter analyze` limpo:
  - **Verificação configurável por órgão**: `RegrasPonto.verificacaoObrigatoria` (migration V22) + toggle no painel (RegrasModal); `GET /api/me/config` informa o app. *`OrgaoRegrasTest#verificacaoObrigatoriaDoOrgaoEhResolvida` + `MeAuthTest#configDoAppRetornaVerificacao`.*
  - **App — verificação no aparelho**: `local_auth` (digital/PIN/desenho via bloqueio do SO; **fallback facial** quando o aparelho não tem bloqueio), gate no `/bater` conforme a config; `MainActivity` → `FlutterFragmentActivity` + permissão `USE_BIOMETRIC`. Sem verificação → botão direto. *(testar em device real).*
  - **Acesso web do servidor**: rota pública `/servidor` (ativação por código → token no navegador → botão único `/api/me/bater`); `apiFetch` envia `X-Device-Token`. Bate ponto pela web no PC ou no navegador do celular.
  - **Prompt do Stitch**: `docs/stitch-prompt-mobile.md` (todas as telas do app + fluxo de verificação; padrão DSGov acessível).
- [x] 11.19 **White-label por prefeitura (logo / nome / cores)** — backend **96/96**, web `build` OK, `flutter analyze` limpo:
  - **Branding por ente**: `Branding` (@Embeddable no Tenant — nome do app, logo URL, cor primária, cor de acento; migration V23); `GET/PUT /api/branding` (RH/tenant-admin) + `GET /api/me/branding` (app), com defaults quando vazio. *`BrandingTest` + `MeAuthTest#brandingDoAppAplicaDefaults`.*
  - **Painel**: página "Identidade visual" (`/identidade`) com nome/logo/cores + pré-visualização; o `AppShell` carrega o branding e aplica a cor primária (`--color-primary`) + logo/nome no header.
  - **App**: `AppTheme.themeFor(cor)` + `corPrimariaApp` (ValueNotifier) → tema reativo com a cor do ente; a home mostra nome + logo do município (busca `/api/me/branding` após ativar). Logo por URL (upload p/ S3 fica como evolução).
  - **2 prompts do Stitch** com direção de arte autoral (anti-"cara de IA"): `docs/stitch-prompt-mobile.md` (conceito "Carteira de Ponto") e `docs/stitch-prompt-web.md` (conceito "Livro-Ponto / Mesa de Registro").

## 12. Backlog — adoção (anti-objeção) e personalização (a implementar)

### 12.1 Adoção / anti-objeção (reduz resistência dos servidores e risco político do prefeito)
- [x] 12.1.1 **Modo adaptação** — FEITO: `RegrasPonto.adaptacaoAte` (data de vigência por órgão, migration V24) + `RegrasPonto.emAdaptacao(data)`; `ApuracaoService` suprime ocorrências que penalizam o servidor (`TipoOcorrencia.penalizaServidor()`: ATRASO/FALTA/SAÍDA_ANTECIPADA) no período, mantendo créditos a favor (hora extra) — reflete no banco de horas, espelho e relatórios; flag `emAdaptacao` em `ApuracaoDia`/`ApuracaoDiaResponse`. **Painel**: campo "Modo adaptação até" no editor de regras do órgão. *`RegrasOrgaoApuracaoTest#modoAdaptacaoSuprimePenalidades*` + `#modoAdaptacaoMantemCreditoDeHoraExtra*`.*
- [x] 12.1.2 **Privacidade explícita no app + trilha pessoal** — FEITO: consentimento ("não rastreamos entre batidas") já existia; **trilha pessoal** agora em `GET /api/me/trilha` (`TrilhaService` agrega correções de marcação + justificativas/atestados do vínculo, com a decisão/quem aprovou, em ordem decrescente) + tela "Meu histórico" no app. *`TrilhaTest` (agrega+ordena; vazia sem eventos)* + `TrilhaEventoItem` fromJson. Servidor vê só o próprio (vínculo derivado do device principal). (Log de "quem *acessou*" os dados — read-audit — fica como evolução.)
- [x] 12.1.3 **Pesquisa de satisfação/feedback** — FEITO: módulo `satisfacao` (`PesquisaSatisfacao` nota 1..5 + comentário, migration V31+RLS); `POST /api/me/satisfacao` (servidor avalia) + `GET /api/satisfacao/resumo` (RH/prefeito: total, média, distribuição) — métrica de aceitação; card "Satisfação dos servidores" na página Adesão; tela "Avaliar" no app. *`SatisfacaoTest`.* Onboarding/FAQ ("por que ponto eletrônico") fica como conteúdo a incluir no app (texto).
- [x] 12.1.4 **Fluxo "esqueci de bater"** — FEITO: módulo `correcao` (`CorrecaoMarcacao` PENDENTE→APROVADA/REJEITADA, migration V28+RLS); servidor solicita via `POST /api/me/correcoes` (+ `GET`), chefia/RH aprova via `/api/correcoes`. Ao aprovar, `RegistroService.registrarCorrecao` cria uma **nova** batida encadeada na cadeia de hash (origem `AJUSTE`, migration V27; `dataHoraServidor` = momento pretendido) — registros nunca são editados. Bloqueia em competência fechada. **App**: tela "Esqueci de bater" (data/hora + tipo + motivo + minhas solicitações). *`CorrecaoTest` + `CorrecaoItem.fromJson` (Flutter).*
- [x] 12.1.5 **Notificações "a seu favor"** — FEITO: `GET /api/me/resumo` (`ResumoMeResponse`: saldo de banco de horas + hora extra acumulada na semana corrente, via `MeService.resumo` somando HORA_EXTRA da apuração de seg→hoje). **App**: tela Banco de horas mostra "Horas extras nesta semana" além do saldo (`ResumoMe`). *`AlertaRiscoTest#resumoRefleteSaldoDeBancoDeHoras` + `ResumoMe.fromJson` (Flutter).*
- [x] 12.1.6 **Abono coletivo por evento** — FEITO: tipo `ABONO_COLETIVO` no calendário oficial (ver 12.4.5) — geral ou por órgão, neutraliza falta na apuração. *`CalendarioTest#abonoColetivoSoValeParaOOrgaoAlvo`.*
- [x] 12.1.7 **Dossiê de conformidade pró-TCM-GO** — FEITO: `DossieService`/`GET /api/relatorios/dossie` empacota IN 008 + AFD/REP-P + integridade + indicadores como defesa do prefeito (ver 12.3.4).
- [x] 12.1.8 **Isonomia** — FEITO: `AdesaoService.porRegime` + `GET /api/relatorios/adesao/regime` mostra a cobertura (vínculos/aderiram/%) por regime, incluindo **COMISSIONADO** (comissionados/chefias/secretários) ao lado de estatutários/celetistas; página "Adesão". *`AdesaoTest`.*
- [x] 12.1.9 **Implantação gradual (piloto por órgão) + painel de adesão** — FEITO: `AdesaoService.porOrgao` + `GET /api/relatorios/adesao/orgao` (vínculos/aderiram/% por órgão, "aderiu" = tem dispositivo ativo); página "Adesão" com barra de progresso por órgão (acompanha o piloto). *`AdesaoTest`.* Controle formal de "órgão em piloto" (flag) fica como evolução — o painel já mostra a adesão real por órgão.
- [x] 12.1.10 **Acessibilidade reforçada** — FEITO (parte codável): **registro por matrícula no totem** (`TotemService` + `POST /api/totem/bater?matricula=`, tipo deduzido, origem TOTEM) para quem não tem smartphone; botão único "por matrícula" na página Totem. *`TotemMatriculaTest`.* Tema acessível p/ idosos (fontes grandes/alto contraste) já no app (11.3); revisão formal de TalkBack/"letra extra" segue como QA de acessibilidade.

### 12.2 Personalização do município (white-label) — evoluções
- [x] 12.2.1 **Upload de logo** — FEITO (sem depender de S3): logo armazenado **no banco** (`tenant_logo` V42 + RLS, 1 por ente), `POST /api/branding/logo` (bytes + Content-Type, rh/tenant-admin) grava e aponta `Branding.logoUrl` para o serviço público `GET /api/publico/branding/{slug}/logo`. Painel: envio de arquivo na Identidade Visual; app resolve URL relativa contra o baseUrl. *`LogoTest` (salva/serve por slug/atualiza/rejeita não-imagem).* (Troca para S3 no futuro = só mudar o `LogoService`.)
- [x] 12.2.2 **Branding na tela de ativação** — FEITO: `AtivacaoService.brandingDoCodigo` + `GET /api/ativacao/branding?codigo=` (pré-auth) resolve o ente pelo código e devolve nome/logo/cores para a tela de ativação mostrar a marca da prefeitura antes de ativar. *`TotemMatriculaTest#brandingDaTelaDeAtivacaoResolveOEntePeloCodigo`.*
- [x] 12.2.3 **Logo do ente nos documentos** — FEITO: `PdfEspelhoService` embute o logo do ente (`TenantLogo`) no cabeçalho do **espelho** e da **declaração** (best-effort). *`ConformidadeTest#espelhoComLogoDoEnteGeraPdf`.* (O **AFD** é arquivo-texto de largura fixa — logo não se aplica.)
- [x] 12.2.4 **Tema escuro do painel** — FEITO: toggle "🌙 Escuro/☀️ Claro" no header do painel; tema via `[data-theme="dark"]` no `<html>` (remapeia a escala de cinza + superfícies em `index.css`), persistido em `localStorage`. (cor de acento além da primária fica como evolução.)
- [x] 12.2.5 **Subdomínio/rota por ente** — FEITO (camada de app; DNS é infra à parte): `Tenant.subdominio` (V43, único) + `PUT /api/branding/subdominio` (rh/tenant-admin) e resolução pública `GET /api/publico/ente/{subdominio}` → `EntePublicoResponse` (slug + identidade visual) p/ o app/login se autoconfigurar. Campo no painel (Identidade Visual). *`SubdominioTest` (define/resolve, unicidade, formato inválido, inexistente).* (E-mail/notificações com a marca do ente = depende de infra SMTP.)

### 12.3 Diferenciais comerciais (convencer o prefeito a adquirir) — 🎯 = "matador" de venda
- [x] 12.3.1 🎯 **Painel de ROI / economia (em R$)** — FEITO: `RoiService` + `GET /api/relatorios/roi` estima a economia (hora extra controlada + queda de absenteísmo) a partir do apurado (BI) × percentuais de redução informados pelo gestor — transparente; card "ROI / economia estimada" na página BI executivo. *`BiExecutivoTest#roiEstimaEconomiaDeHoraExtra`.* (fim do ponto-por-colega = efeito da biometria/geofence já entregues.)
- [x] 12.3.2 🎯 **BI executivo do prefeito** — FEITO: `BiExecutivoService` + `GET /api/relatorios/bi` agrega por secretaria (órgão) vínculos, faltas (absenteísmo), hora extra e vínculos sem batida ("fantasma"); página "BI executivo" com totais + tabela por órgão (ranking por hora extra). *`BiExecutivoTest`.* (custo de hora extra em R$ = minutos × custo/hora no simulador 12.5.5.)
- [ ] 12.3.3 🎯 **Integração real com a folha de pagamento** (conectores reais; hoje há stub) — fim do retrabalho/erro do RH.
- [x] 12.3.4 🎯 **Escudo jurídico empacotado** — FEITO: `DossieService` + `GET /api/relatorios/dossie` reúne num pacote IN 008, AFD (Portaria 671 + hash SHA-256), integridade da cadeia (tamper-evidence), AEJ, LGPD e auditoria — checklist de escudos + prazo TCM; card "Dossiê de defesa" na página Conformidade. *`ConformidadeTest#dossieDeConformidadeEmpacotaAsEvidencias`.* (assinatura ICP-Brasil e eSocial reais são etapas externas já documentadas.)
- [x] 12.3.5 **Relatórios automáticos + alerta de prazo do TCM** — FEITO parcial: o dossiê (`/api/relatorios/dossie`) calcula o **prazo de submissão** (dia 15 do mês seguinte) e os **dias restantes**; geração/envio agendado automático (cron) é etapa de operação/infra.
- [x] 12.3.6 **Transparência ativa / portal de dados abertos** — FEITO: `TransparenciaService` + `GET /api/publico/transparencia/{slug}` (público, sem auth) resolve o ente pelo slug e devolve frequência **agregada e anônima** (servidores, vínculos, registros no período) — alimenta um portal de dados abertos. *`TransparenciaTest` (valida resolução por slug + isolamento de tenant sem contexto prévio).*
- [x] 12.3.7 **Comunicados oficiais (broadcast) no app** — FEITO: módulo `comunicado` (`Comunicado` geral ou por órgão, migration V25 + RLS); `POST/GET /api/comunicados` (RH/tenant-admin) + `GET /api/me/comunicados` (servidor vê gerais + os do seu órgão). **Painel**: página "Comunicados" (publicar com destinatário geral/órgão + lista). **App**: tela "Comunicados" + `ComunicadoItem`/`listarComunicados`. *`ComunicadoTest` + `ComunicadoItem.fromJson` (Flutter).*
- [x] 12.3.8 **Carteira funcional digital** no app — FEITO: `CarteiraService` + `GET /api/me/carteira` compõe nome/CPF/matrícula/cargo/regime/órgão + identidade do ente (white-label); tela "Carteira funcional" no app (cartão com a cor do ente). *`CarteiraTest` + `CarteiraDigital.fromJson` (Flutter).*
- [x] 12.3.9 **Escalas complexas — 12×36 FEITO**: `ApuracaoService.horarioEsperado` reconhece `TipoJornada.ESCALA_12X36` — trabalha em dias alternados (12h+36h = ciclo 48h) a partir da **data de início da escala (âncora)**; dia de folga não tem horário esperado (sem falta; trabalho vira hora extra). Hint no painel de Jornadas. *`RotacaoEscalaTest` (paridade dos dias + trabalho na folga vira HE).* (Plantão/sobreaviso = 12.4.3; professores/hora-atividade = 12.5.8.)
- [x] 12.3.10 **Múltiplos locais / geofence multi-ponto** (servidores volantes: saúde, fiscalização, obras) — FEITO: módulo `geofence_local` (V38 + RLS) — cada órgão pode ter várias **áreas de referência**; `GeofenceLocalService` + `GET/POST/DELETE /api/lotacoes/{id}/locais` (admin). O `RegistroService` combina a **cerca primária do órgão + os locais volantes**: a batida só fica `foraDaCerca` quando está fora de **todas** as áreas (teletrabalho desativa; segue silencioso — não bloqueia/avisa). Painel: botão "📍 Locais" no órgão (lista + adicionar + remover, link Google Maps). *`GeofenceLocalTest` (CRUD, fora-de-todas, combina com cerca primária, isolamento entre entes).*
- [x] 12.3.11 **App do gestor** — FEITO: a chefia decide pelo app. `GestorService` resolve a identidade do gestor pelo dispositivo (vínculo→servidor→chefia, incluindo delegação) e expõe `GET /api/me/chefia` (resumo: é gestor? quantas pendências), `GET /api/me/chefia/pendentes` e `POST /api/me/chefia/justificativas/{id}/aprovar|rejeitar`. **Autorização**: só atua sobre justificativas de vínculos do **próprio time** (senão 403 `AcessoNegadoException`) e a aprovação respeita a **alçada do gestor** (`aprovarComAlcada(...,false)`). App: tela "Gestão da equipe" (aprovar/rejeitar), exibida só para chefias. *`GestorTest` (souGestor; pendentes só do time; aprova do time; 403 fora do time; alçada).* (Alertas proativos no app = evolução; já há `AlertaRiscoService` no painel.)
- [x] 12.3.12 **Detecção de anomalias (heurística, ligável no painel)** — FEITO (opção 1): `DetectorAnomalia` (POJO explicável: valor > média×fator **E** piso) + `AnomaliaService.detectar(competencia)` sinaliza **hora extra atípica** por vínculo, **gated pela funcionalidade `ANOMALIAS`** (desligada por padrão). `GET /api/relatorios/anomalias?competencia=`. Base reutilizável de **feature-flags por ente**: V44 `funcionalidade_flag` (+RLS), enum `Funcionalidade`, `FuncionalidadeService`, `GET/PUT /api/funcionalidades` → rh/tenant-admin; painel web **Funcionalidades** (toggles + demo ao vivo das anomalias). *`DetectorAnomaliaTest`, `AnomaliaGatingTest`, `FuncionalidadeTest`.* (Previsão de absenteísmo = 12.4.12, ainda IA.)
- [x] 12.3.13 **Onboarding self-service do ente** — FEITO (fluxo seguro pedido→aprovação→provisionamento): endpoint **público** `POST /api/publico/onboarding` (`OnboardingPublicoController`, rate-limit 5/h por IP→429) grava uma `SolicitacaoEnte` **PENDENTE** (tabela global `solicitacao_ente` V41, sem RLS — pré-tenant); **não cria tenant**. O operador aprova/rejeita em `/api/onboarding/solicitacoes[/{id}/aprovar|rejeitar]` (role `operador`) — aprovar chama `OnboardingService.provisionar` (cria tenant + lotação inicial). Página pública `/aderir` + página do operador "Adesão de entes". *`SolicitacaoEnteTest` (pendente→aprovar provisiona; slug pendente duplicado; não aprova 2×).*
- *Obs.: anti-fraude facial deixado de fora a pedido; o framework segue disponível (5.x/11.14).*

### 12.4 Funcionalidades úteis adicionais (RH / gestão)
**Sem IA:**
- [x] 12.4.1 **Gestão de férias e licenças** — FEITO: módulo `ausencia` (`AusenciaProgramada` tipos FERIAS/LICENCA_*; migration V29+RLS); a apuração trata os dias da ausência como não úteis (zera horário esperado → sem falta — `ApuracaoService` combina calendário + ausência); `/api/ausencias` (CRUD) + `/api/ausencias/cobertura` (cobertura da equipe por órgão — quem está ausente e quando, p/ não deixar o setor descoberto); `GET /api/me/ausencias` (servidor vê as próprias). **Painel**: página "Férias / Licenças" (programar + lista + cobertura). **App**: tela "Minhas férias". *`AusenciaTest` (4 casos) + `AusenciaItem.fromJson` (Flutter).* Saldo/períodos aquisitivos e alertas de vencimento ficam como evolução (depende de regra por categoria/admissão).
- [x] 12.4.2 **Teletrabalho / home office** — FEITO: flag `RegrasPonto.teletrabalho` por órgão (migration V35); em teletrabalho a **geofence não se aplica** (o servidor bate de qualquer lugar — `RegrasEfetivas.geofence()` retorna vazio), sem `fora_da_cerca`. Toggle no editor de regras do órgão. *`RegrasOrgaoApuracaoTest#teletrabalhoIgnoraGeofence`.* Regras por atividade/meta ficam como evolução.
- [x] 12.4.3 **Plantões e sobreaviso** — FEITO: **plantão** via `TipoJornada.PLANTAO`/escala (turno longo) e rotativo via 12×36 (12.3.9); **sobreaviso** (on-call) como módulo próprio `sobreaviso` (V39 + RLS): `SobreavisoService` + `GET/POST/DELETE /api/sobreaviso` (RH/chefia) — horas à parte (não afetam a apuração), somadas por competência e **exportadas para a folha** (nova coluna `sobreavisoMin` no `FolhaService`, p/ pagamento em rubrica própria). Painel: botão "⏰ Sobreaviso" por vínculo. *`SobreavisoTest` (soma por competência, listagem, remoção, coluna na folha).* (Troca de plantão entre colegas com aprovação = evolução.)
- [x] 12.4.4 **Apropriação de horas por projeto/convênio/fonte** — FEITO: módulo `projeto` (`Projeto` + `ApropriacaoHoras`, migration V34+RLS); `/api/projetos` (CRUD + `/apropriacoes` + `/relatorio` por competência somando horas por projeto/fonte); página "Projetos / Convênios". *`ProjetoTest`.*
- [x] 12.4.5 **Calendário oficial do município** — FEITO: módulo `calendario` (`EventoCalendario` tipo FERIADO/PONTO_FACULTATIVO/ABONO_COLETIVO, geral ou por órgão; migration V26 + RLS); `CalendarioService.diaNaoUtilParaVinculo` torna a data dia não útil na apuração (`ApuracaoService` zera o horário esperado → sem falta, trabalho vira hora extra); `/api/calendario` (CRUD, RH/admin). **Painel**: página "Calendário oficial" (adicionar/listar/remover por competência). *`CalendarioTest` (feriado neutraliza falta, trabalho no feriado vira hora extra, abono por órgão).* A **ciência eletrônica do espelho** já existe (`POST /api/espelho/ciencia`).
- [x] 12.4.6 **Diárias e deslocamentos** — FEITO: tipos `VIAGEM` (deslocamento a serviço/diária) e `CAPACITACAO` no módulo de ausências (migration V32 atualiza o check); a apuração trata o período como não útil (não gera falta), igual às férias/licenças; rótulos no painel "Férias/Licenças". *`AusenciaTest#viagemAServicoTambemNeutralizaFalta`.* O cálculo financeiro da diária é do sistema de RH/folha (externo).
- [x] 12.4.7 **Frequência de estagiários, temporários e terceirizados** — FEITO: regimes `ESTAGIARIO`/`TEMPORARIO`/`TERCEIRIZADO` (migration V33 atualiza o check) disponíveis no cadastro de vínculo (dropdown no painel) e nos relatórios de adesão/isonomia/BI; as regras de ponto são por órgão/escala (independentes do regime), então cada contrato pode ter regras próprias via lotação. *coberto por `ProjetoTest` (vínculo ESTAGIARIO).*

**Com IA:**
- [x] 12.4.8 **Assistente virtual no app** — FEITO (opção 2, **seam de IA**): `POST /api/me/assistente` (role servidor) via `AssistenteService`, **gated por `IA_ASSISTENTE` + provedor**. Seam `IaProvider` (default no-op `IaIndisponivel`; produção = bean `@Primary`, mesmo padrão do `AssinaturaService`). Responde "indisponível" de forma graciosa com a função desligada ou sem provedor. *`IaGatingTest`, `IaSeamTest`.*
- [x] 12.4.9 **OCR de atestados** — FEITO (opção 2): `POST /api/me/atestados/ocr` (multipart, role servidor) via `OcrAtestadoService` → `IaProvider.lerAtestado` (retorna `OcrAtestado`: CID/início/dias/profissional). Gated por `IA_OCR` + provedor.
- [x] 12.4.10 **Resumo inteligente para o gestor** — FEITO (opção 2): `GET /api/relatorios/resumo-ia?competencia=` (rh/controladoria/tenant-admin) via `ResumoIaService` → `IaProvider.resumirGestao` sobre os indicadores do mês. Gated por `IA_RESUMO` + provedor.
- [x] 12.4.11 **Otimização de escala** — FEITO (heurística, sem IA): POJO `OtimizadorEscala` (distribuição justa por menor carga + teto de dias consecutivos, respeitando ausências programadas) + `OtimizacaoEscalaService` + `GET /api/escalas/sugestao?lotacaoId=&competencia=&cobertura=&maxConsecutivos=` (sugere, não aplica). *`OtimizadorEscalaTest`.*
- [x] 12.4.12 **Previsão de absenteísmo + projeção de custo de hora extra** — FEITO (heurística): POJO `ProjecaoAbsenteismo` (média + tendência, ≥0) sobre os 3 meses anteriores (faltas + HE via `BiExecutivoService`); `PrevisaoService` + `GET /api/relatorios/previsao?competencia=&custoHora=`. *`ProjecaoAbsenteismoTest`.*
- [x] 12.4.13 **Acessibilidade por voz** — FEITO (app Flutter): `VozService` (flutter_tts fala o status; speech_to_text ouve "bater ponto") + `ComandoVoz` (reconhecimento puro, testado) + botão "Bater por voz" e fala do resultado no home; permissões RECORD_AUDIO (Android) + NSMicrophone/NSSpeechRecognition (iOS). *`comando_voz_test`.*
- [x] 12.4.14 **Análise de sentimento** — FEITO (IA, opção 2): `IaProvider.analisarSentimento` (Chat Completions → JSON contagens + resumo) + `SentimentoService` gated por `IA_SENTIMENTO` + provedor; `GET /api/satisfacao/sentimento`; toggle no painel. *`SentimentoGatingTest`, `OpenAiIaProviderTest`.*
- [x] 12.4.15 **Detecção de inconsistências de jornada** — FEITO (regra, sem IA): `InconsistenciaService` + `GET /api/relatorios/inconsistencias` aponta dias com nº ímpar de marcações (intervalo aberto = esquecimento de batida); seção na página Conformidade. *`RelatoriosControleTest`.*

### 12.5 "Matadoras" — protegem o erário e o mandato do prefeito (🎯 = prioridade máxima)
- [x] 12.5.1 🎯 **Alerta de limite da LRF** — FEITO: `LimiteLrf` (POJO testável: 54% da RCL p/ Executivo + prudencial 95% ≈ 51,3%; classifica OK/ALERTA/PRUDENCIAL/ESTOURO) usado pelo simulador (12.5.5). *`LimiteLrfTest`.* RCL/gasto atual entram como parâmetro (dado externo da contabilidade).
- [x] 12.5.2 🎯 **Caça a "servidor fantasma" + acúmulo ilícito de cargos** — FEITO (regras, sem facial): `DetectorAcumulo` (POJO testável: janelas de horário sobrepostas no mesmo dia da semana) + `DeteccaoService` cruza vínculos ativos do mesmo servidor com jornadas sobrepostas (acúmulo) e aponta vínculos ativos sem nenhuma batida no período (fantasma); `GET /api/relatorios/acumulo`. **Painel**: seção "Detecção de irregularidades" na página Conformidade. *`DetectorAcumuloTest` (unit) + `DeteccaoTest` (integração).* IA preditiva fica como evolução (12.4.12).
- [~] 12.5.3 🎯 **Migração "zero-dor"** — PARCIAL (parte codável FEITA; REP-C físico FORA a pedido do cliente): `MigracaoService` importa **saldo inicial de banco de horas** via CSV `matricula;saldoMinutos` (novo `TipoLancamento.MIGRACAO`, migration V45, CHECK atualizado), **idempotente** (re-rodar não dobra); `POST /api/migracao/banco-horas` (rh/tenant-admin). *`MigracaoServiceTest`.* Import de servidores já existia (12.6/lote 20). **Leitura do relógio físico (REP-C) não incluída** — o cliente não trabalha com ponto físico inicialmente. **Endurecido pós-revisão adversarial:** cada linha importa em transação própria (`MigracaoLinhaService` @Transactional REQUIRES_NEW + saveAndFlush) → falha de uma linha não reverte/contamina o lote e é atribuída à linha certa; **índice único parcial** `uq_banco_horas_migracao` (V46, `where tipo='MIGRACAO'`) fecha a corrida check-then-act (2ª importação concorrente vira "ignorada" via DataIntegrityViolationException); tolera **BOM/linha em branco/cabeçalho fora da 1ª linha** (CSV UTF-8 do Excel).
- [x] 12.5.4 **Super-app do servidor** — FEITO parcial: o app já reúne ponto (botão único), férias ("Minhas férias"), comunicados, declaração de frequência (PDF), carteira funcional, banco de horas, justificativas, "esqueci de bater", avaliação e privacidade num app só. Falta o **contracheque** (depende de integração externa da folha — 12.3.3).
- [x] 12.5.5 **Simulador orçamentário de hora extra/pessoal** — FEITO: `SimuladorService` + `GET /api/relatorios/simulador` ("autorizar X horas extras custa Y e me deixa a Z% da RCL", com risco LRF); card na página "BI executivo". *`SimuladorServiceTest`.*
- [x] 12.5.6 **Blindagem TCM (central de conformidade)** — FEITO: o dossiê (`/api/relatorios/dossie`) é a central — relatórios prontos (IN008/AFD/AEJ/integridade) + alerta de prazo + checklist de defesa contra glosa.
- [x] 12.5.7 **Declaração de frequência self-service** — FEITO (`GET /api/me/declaracao`, PDF/OpenPDF; teste `ConformidadeTest#declaracaoDeFrequenciaEmPdf`).
- [x] 12.5.8 **Pisos/jornadas legais — hora-atividade do magistério FEITO**: `Jornada.horaAtividadeMin` (V40, opcional) = minutos semanais de hora-atividade; `PisoMagisterioService` + `GET /api/jornadas/piso-magisterio` avalia as jornadas que a declararam e sinaliza as **abaixo do mínimo legal (1/3 da carga)**. Campo no form de Jornadas + seção "Hora-atividade (Lei do Piso)" em Conformidade (badge Atende/Abaixo). *`PisoMagisterioTest` (1/3 atende, 25% não atende, sem-HA fora do relatório).* (Outras categorias/saúde = mesma mecânica, configurando a hora-atividade.)
- *Obs.: recadastramento/prova de vida deixado de fora a pedido.*

### 12.6 Facilitadores do RH e do controle interno
**RH (eliminar trabalho manual):**
- [x] 12.6.1 **Caixa de entrada de aprovações + aprovação/recusa em lote** — FEITO (justificativas): `JustificativaService.aprovarEmLote/rejeitarEmLote` (idempotente — pula inexistentes/já decididas, auditando cada decisão); `POST /api/justificativas/lote/aprovar|rejeitar` (`DecisaoLoteRequest`). **Painel**: seleção por checkbox + "Aprovar/Recusar selecionados" na página Atestados (a caixa de entrada é o `/pendentes`). *`JustificativaTest#aprovacaoEmLoteDecideVariasEIgnoraJaDecididas`.* Lote de ajustes/banco de horas fica como evolução.
- [x] 12.6.2 **Fechamento de competência em lote + painel "o que falta fechar"** — FEITO: `CompetenciaService.fecharEmLote` (idempotente, pula já fechadas, audita cada) + `POST /api/espelho/fechar-lote`; `PendenciaFechamentoService` (read-model agrupado por órgão com totais) + `GET /api/espelho/pendentes` (`PendenciaFechamentoResponse`). **Painel**: seção "O que falta fechar" na página Espelho (totais + pendências por órgão + fechar selecionados em lote). *`EspelhoFechamentoTest#fechamentoEmLoteEsvaziaOPainelDePendencias`.*
- [x] 12.6.3 **Templates de jornada/escala reutilizáveis + aplicação em massa** — FEITO: a Jornada já é o template reutilizável; `EscalaService.atribuirEmLote` + `POST /api/escalas/lote` aplica uma jornada a vários vínculos de uma vez, pulando quem tem vigência sobreposta (lote robusto). *`JornadaEscalaTest#aplicaJornadaEmMassaPulandoVinculoComVigenciaSobreposta`.*
- [x] 12.6.4 **Correção de marcação em lote** — FEITO: `CorrecaoService.corrigirEmLote` + `POST /api/correcoes/lote` (RH cria várias marcações de um vínculo de uma vez, cada uma já aprovada → nova batida origem AJUSTE encadeada + auditoria `CORRECAO_MARCACAO`); painel "Correções de marcação" (caixa de pendentes com aprovar/recusar + correção direta em lote). *`CorrecaoTest#correcaoEmLoteDoRhCriaMarcacoesERefleteNaApuracao`.*
- [x] 12.6.5 **Onboarding guiado do servidor** — FEITO: `OnboardingServidorService` + `POST /api/servidores/onboarding` cadastra o servidor (com vínculo) e já devolve o **código de ativação** do app num passo só. *`OnboardingServidorTest`.* (atribuição de jornada/escala permanece nas telas próprias.)
- [x] 12.6.6 **Relatórios prontos exportáveis** — FEITO (CSV/PDF): folha de frequência (`/api/relatorios/frequencia/csv`), espelho e declaração em PDF (`/api/relatorios/espelho/pdf`, `/api/me/declaracao`), AFD/AEJ (`.txt`), e **abonos** CSV (`/api/relatorios/abonos/csv`). Export Excel nativo e CSV de banco de horas ficam como evolução (CSV já cobre).
- [x] 12.6.7 **Lembretes automáticos de pendência** — FEITO: `LembretePendenciaService` + `POST /api/notificacoes/lembretes` notifica os servidores com competência **fechada e sem ciência**; dispara sob demanda (agendamento automático = cron na operação). *`TrilhaLembreteTest#lembreteDeCienciaNotificaServidorComCompetenciaFechada`.* Lembrete a gestor com aprovação parada fica como evolução (mesma mecânica).
- [x] 12.6.8 **Delegação de aprovação** — FEITO: módulo `delegacao` (`Delegacao` delegante→delegado por período; migration V30+RLS); `/api/delegacoes` (criar/listar/revogar); `JustificativaService.pendentesDaChefia` expande as chefias do substituto com os gestores que lhe delegaram (vigência por data), via `DelegacaoService.delegantesAtivosPara`. **Painel**: página "Delegação de aprovação". *`DelegacaoTest` (substituto vê e perde ao revogar; fora de vigência não vale; valida parâmetros).*

**Controle interno / controladoria:**
- [x] 12.6.9 **Verificador de integridade em 1 clique** — FEITO (`GET /api/relatorios/integridade`; `IntegridadeService` recalcula a cadeia e aponta o NSR rompido; teste `RegistroPontoTest#cadeiaDeIntegridadeEhVerificavel`).
- [x] 12.6.10 **Alertas de risco proativos** — FEITO parcial: `AlertaRiscoService` + `GET /api/relatorios/alertas` aponta **ajustes manuais de banco de horas** (tipo AJUSTE — vetor de abuso) e **batidas fora da cerca** no período; seção "Alertas de risco" na página Conformidade. *`AlertaRiscoTest`.* Hora extra atípica já sai por vínculo no relatório de frequência; "servidor sem bater" sai na detecção (12.5.2).
- [x] 12.6.11 **Indicadores** — FEITO parcial (`GET /api/relatorios/indicadores`: total de vínculos, dispositivos ativos, % de adesão, registros no período; teste `ConformidadeTest#indicadoresDeGestao`). Falta: ranking de ajustes por gestor + exceções por órgão.
- [x] 12.6.12 **Conferência cruzada folha × frequência** — FEITO: `ConferenciaFolhaService` + `POST /api/integracoes/conferencia` compara a hora extra paga e as faltas descontadas (informadas pela folha) com o apurado, sinalizando divergências. *`ConferenciaTest`.* Os valores da folha entram como dado externo (integração real da folha = etapa externa).
- [x] 12.6.13 **Segregação de funções + alçadas de aprovação** — FEITO: `JustificativaService.aprovarComAlcada(id, motivo, aprovadorEhRh)` — gestor só aprova ausências de até `ALCADA_GESTOR_DIAS` (5) dias; acima, sobe para RH/controle (`ConflitoException`). O `JustificativaController` deriva o perfil do `SecurityContext` (RH/controladoria/tenant-admin = sem limite; gestor = limitado; modo aberto/dev = sem limite). Nota da alçada na tela de Atestados. *`JustificativaTest#alcadaLimitaGestorEmAusenciaLonga`.* (teto configurável por órgão fica como evolução.)
- [x] 12.6.14 **Bloqueio pós-fechamento** — FEITO (já entregue em 7.3/7.4): competência fechada bloqueia novas justificativas e correções de marcação (`ConflitoException`); reabertura é controlada e auditada (`REABERTURA` com motivo). *`EspelhoFechamentoTest`, `CorrecaoTest#correcaoEmCompetenciaFechadaEhRejeitada`.*
- [x] 12.6.15 **Relatório de abonos e exceções** — FEITO: `AbonoRelatorioService` + `GET /api/relatorios/abonos` (+ `/csv`) lista justificativas do período com tipo/período/status/decisão (motivo + quando + `aprovadorId`); seção na página Conformidade. *`RelatoriosControleTest#relatorioDeAbonosTrazAJustificativaComDecisao`.* "Quem aprovou" (nome) preenche quando o login admin estiver mapeado ao `aprovadorId`.
- [x] 12.6.16 **Trilha completa (antes-depois)** — FEITO: `LotacaoService` audita alteração de **regras do órgão** (`REGRAS_ORGAO` com resumo antes/depois) e de **chefia** (`CHEFIA_ALTERADA`); aprovações já eram auditadas (`DECISAO_JUSTIFICATIVA`, `FECHAMENTO`, `REABERTURA`). Visível na página Auditoria. *`TrilhaLembreteTest#alteracaoDeRegrasGeraTrilhaAntesDepois`.*
